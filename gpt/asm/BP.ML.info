GLOBAL { STARTADDR $C000; INPUTOFFS 2; CPU "6502"; COMMENTS 3; LABELBREAK 0; };
RANGE { START $CF2E; END $CFF1; TYPE TextTable; };
RANGE { START $CFF2; END $CFFB; TYPE ByteTable; };
RANGE { START $CFFC; END $CFFE; TYPE TextTable; };
LABEL { ADDR $0022; NAME "BASIC_INDEX"; SIZE 2; COMMENT "BASIC indirect work pointer."; };
LABEL { ADDR $002D; NAME "BASIC_VARTAB"; SIZE 2; COMMENT "Start of BASIC scalar variables."; };
LABEL { ADDR $002F; NAME "BASIC_ARYTAB"; SIZE 2; COMMENT "Start of BASIC arrays."; };
LABEL { ADDR $0031; NAME "BASIC_STREND"; SIZE 2; COMMENT "End of BASIC arrays."; };
LABEL { ADDR $0033; NAME "BASIC_FRETOP"; SIZE 2; COMMENT "Bottom of string storage."; };
LABEL { ADDR $0037; NAME "BASIC_MEMSIZ"; SIZE 2; COMMENT "BASIC memory ceiling."; };
LABEL { ADDR $007A; NAME "BASIC_TXTPTR"; SIZE 2; COMMENT "BASIC parser position; temporarily redirected to embedded names."; };
LABEL { ADDR $0100; NAME "BASIC_PRINT_BUFFER"; SIZE 1; COMMENT "ASCII number buffer used by the BASIC ROM."; };
LABEL { ADDR $02A7; NAME "SavedTextPtr"; SIZE 2; COMMENT "Saved caller parser position."; };
LABEL { ADDR $02A9; NAME "InputCount"; SIZE 1; COMMENT "P1: number of input processing elements."; };
LABEL { ADDR $02AC; NAME "PatternCount"; SIZE 1; COMMENT "NP: number of training patterns."; };
LABEL { ADDR $02AD; NAME "RateOffset"; SIZE 2; COMMENT "RA: relative to BASIC_VARTAB after initialization."; };
LABEL { ADDR $02BB; NAME "PatternIndex"; SIZE 1; COMMENT "Training pattern number; zero selects recognition scratch input."; };
LABEL { ADDR $02CD; NAME "WorkFloat"; SIZE 5; COMMENT "Temporary five-byte float; also reused as pointer/count scratch."; };
LABEL { ADDR $02DD; NAME "FilenameBuffer"; SIZE 1; COMMENT "Up to 20 filename characters followed by comma and R or W."; };
LABEL { ADDR $0334; NAME "ElementPtr"; SIZE 2; COMMENT "Absolute address of a BASIC five-byte array element."; };
LABEL { ADDR $0336; NAME "IndexI"; SIZE 1; COMMENT "Outer loop index; reused by I/O loops."; };
LABEL { ADDR $0338; NAME "IndexJ"; SIZE 1; COMMENT "Inner loop index; reused by I/O loops."; };
LABEL { ADDR $03FD; NAME "MatrixBound"; SIZE 1; COMMENT "First BASIC dimension upper bound, not element count."; };
LABEL { ADDR $A474; NAME "BASIC_READY"; SIZE 1; COMMENT "Return to READY."; };
LABEL { ADDR $A67A; NAME "BASIC_RESET_STACK"; SIZE 1; COMMENT "Reset BASIC stack."; };
LABEL { ADDR $A838; NAME "BASIC_BREAK"; SIZE 1; COMMENT "BASIC break handling."; };
LABEL { ADDR $AB24; NAME "BASIC_PRINT_STRING"; SIZE 1; COMMENT "Print string."; };
LABEL { ADDR $AD8A; NAME "BASIC_EVAL_NUMBER"; SIZE 1; COMMENT "Evaluate numeric expression."; };
LABEL { ADDR $AD8F; NAME "BASIC_REQUIRE_STRING"; SIZE 1; COMMENT "Require string value."; };
LABEL { ADDR $AD9E; NAME "BASIC_EVAL"; SIZE 1; COMMENT "Evaluate expression."; };
LABEL { ADDR $AEFD; NAME "BASIC_COMMA"; SIZE 1; COMMENT "Consume required comma."; };
LABEL { ADDR $B081; NAME "BASIC_DIM"; SIZE 1; COMMENT "Dimension arrays."; };
LABEL { ADDR $B08B; NAME "BASIC_VARIABLE"; SIZE 1; COMMENT "Find/create variable."; };
LABEL { ADDR $B1AA; NAME "BASIC_TO_WORD"; SIZE 1; COMMENT "Convert FAC to integer."; };
LABEL { ADDR $B248; NAME "BASIC_BAD_QUANTITY"; SIZE 1; COMMENT "Illegal quantity error."; };
LABEL { ADDR $B6A6; NAME "BASIC_STRING_DATA"; SIZE 1; COMMENT "String length/pointer."; };
LABEL { ADDR $B79E; NAME "BASIC_GET_BYTE"; SIZE 1; COMMENT "Parse byte into X."; };
LABEL { ADDR $B7A1; NAME "BASIC_FAC_TO_BYTE"; SIZE 1; COMMENT "Convert FAC to byte."; };
LABEL { ADDR $B850; NAME "FP_MEM_MINUS_FAC"; SIZE 1; COMMENT "FAC = memory minus FAC."; };
LABEL { ADDR $B853; NAME "FP_ARG_MINUS_FAC"; SIZE 1; COMMENT "FAC = ARG minus FAC."; };
LABEL { ADDR $B867; NAME "FP_ADD_MEM"; SIZE 1; COMMENT "FAC += memory."; };
LABEL { ADDR $B86A; NAME "FP_ADD_ARG"; SIZE 1; COMMENT "FAC += ARG."; };
LABEL { ADDR $BA28; NAME "FP_MUL_MEM"; SIZE 1; COMMENT "FAC *= memory."; };
LABEL { ADDR $BA8C; NAME "FP_LOAD_ARG"; SIZE 1; COMMENT "Load ARG from memory."; };
LABEL { ADDR $BAE2; NAME "FP_TIMES_TEN"; SIZE 1; COMMENT "FAC *= 10."; };
LABEL { ADDR $BB0F; NAME "FP_MEM_DIV_FAC"; SIZE 1; COMMENT "FAC = memory / FAC."; };
LABEL { ADDR $BBA2; NAME "FP_LOAD"; SIZE 1; COMMENT "Load FAC from memory."; };
LABEL { ADDR $BBD4; NAME "FP_STORE"; SIZE 1; COMMENT "Store FAC at X/Y."; };
LABEL { ADDR $BBFC; NAME "FP_ARG_TO_FAC"; SIZE 1; COMMENT "Copy ARG into FAC."; };
LABEL { ADDR $BC0C; NAME "FP_FAC_TO_ARG"; SIZE 1; COMMENT "Copy rounded FAC into ARG."; };
LABEL { ADDR $BC3C; NAME "FP_FROM_BYTE"; SIZE 1; COMMENT "Convert A to FAC."; };
LABEL { ADDR $BC5B; NAME "FP_COMPARE"; SIZE 1; COMMENT "Compare FAC with memory."; };
LABEL { ADDR $BCCC; NAME "FP_INT"; SIZE 1; COMMENT "Integer part of FAC."; };
LABEL { ADDR $BDDD; NAME "FP_FORMAT"; SIZE 1; COMMENT "Format FAC as ASCII."; };
LABEL { ADDR $BFED; NAME "FP_EXP"; SIZE 1; COMMENT "Exponential of FAC."; };
LABEL { ADDR $E097; NAME "FP_RND"; SIZE 1; COMMENT "BASIC random function."; };
LABEL { ADDR $FFBA; NAME "K_SETLFS"; SIZE 1; COMMENT "Set file/device/channel."; };
LABEL { ADDR $FFBD; NAME "K_SETNAM"; SIZE 1; COMMENT "Set filename."; };
LABEL { ADDR $FFC0; NAME "K_OPEN"; SIZE 1; COMMENT "Open file."; };
LABEL { ADDR $FFC3; NAME "K_CLOSE"; SIZE 1; COMMENT "Close file."; };
LABEL { ADDR $FFC6; NAME "K_CHKIN"; SIZE 1; COMMENT "Select input."; };
LABEL { ADDR $FFC9; NAME "K_CHKOUT"; SIZE 1; COMMENT "Select output."; };
LABEL { ADDR $FFCC; NAME "K_CLRCHN"; SIZE 1; COMMENT "Restore default channels."; };
LABEL { ADDR $FFCF; NAME "K_CHRIN"; SIZE 1; COMMENT "Read byte."; };
LABEL { ADDR $FFD2; NAME "K_CHROUT"; SIZE 1; COMMENT "Write byte."; };
LABEL { ADDR $FFE1; NAME "K_STOP"; SIZE 1; COMMENT "Poll RUN/STOP."; };
LABEL { ADDR $FFE7; NAME "K_CLALL"; SIZE 1; COMMENT "Close all channels."; };
LABEL { ADDR $02AA; NAME "HiddenCount"; SIZE 1; COMMENT "P2: hidden processing elements."; };
LABEL { ADDR $02AB; NAME "OutputCount"; SIZE 1; COMMENT "P3: output processing elements."; };
LABEL { ADDR $02AF; NAME "EpsilonOffset"; SIZE 2; COMMENT "EP: stopping tolerance, relative to BASIC_VARTAB."; };
LABEL { ADDR $02B1; NAME "MomentumOffset"; SIZE 2; COMMENT "MO: learning momentum, relative to BASIC_VARTAB."; };
LABEL { ADDR $02B3; NAME "O2Offset"; SIZE 2; COMMENT "O2(0), relative to BASIC_ARYTAB after initialization."; };
LABEL { ADDR $02B5; NAME "O3Offset"; SIZE 2; COMMENT "O3(0), relative to BASIC_ARYTAB."; };
LABEL { ADDR $02B7; NAME "E2Offset"; SIZE 2; COMMENT "E2(0): hidden-layer deltas, relative to BASIC_ARYTAB."; };
LABEL { ADDR $02B9; NAME "E3Offset"; SIZE 2; COMMENT "E3(0): output-layer deltas, relative to BASIC_ARYTAB."; };
LABEL { ADDR $02BF; NAME "W1Offset"; SIZE 2; COMMENT "W1(0,0): input-to-hidden weights, relative to BASIC_ARYTAB."; };
LABEL { ADDR $02C1; NAME "W2Offset"; SIZE 2; COMMENT "W2(0,0): hidden-to-output weights, relative to BASIC_ARYTAB."; };
LABEL { ADDR $02C3; NAME "M1Offset"; SIZE 2; COMMENT "M1(0,0): previous W1 changes, relative to BASIC_ARYTAB."; };
LABEL { ADDR $02C5; NAME "M2Offset"; SIZE 2; COMMENT "M2(0,0): previous W2 changes, relative to BASIC_ARYTAB."; };
LABEL { ADDR $02C7; NAME "TeacherOffset"; SIZE 2; COMMENT "T(0,0), relative to BASIC_ARYTAB."; };
LABEL { ADDR $02C9; NAME "InputOffset"; SIZE 2; COMMENT "IN(0,0), relative to BASIC_ARYTAB."; };
LABEL { ADDR $02CB; NAME "ErrorOffset"; SIZE 2; COMMENT "E(0): per-pattern half squared error, relative to BASIC_ARYTAB."; };
LABEL { ADDR $02D2; NAME "TotalErrorOffset"; SIZE 2; COMMENT "TE scalar, relative to BASIC_VARTAB."; };
LABEL { ADDR $02D4; NAME "TotalErrorPtr"; SIZE 2; COMMENT "Absolute pointer to TE while training."; };
LABEL { ADDR $02D6; NAME "EpsilonPtr"; SIZE 2; COMMENT "Absolute pointer to EP while training."; };
LABEL { ADDR $02D8; NAME "ErrorScratch"; SIZE 5; COMMENT "Temporary teacher-output difference before squaring."; };
LABEL { ADDR $03FC; NAME "ShowError"; SIZE 1; COMMENT "Nonzero enables printing of TE after each complete training pass."; };
LABEL { ADDR $C000; NAME "BP_Init"; SIZE 1; COMMENT "SYS 49152,p1,p2,p3,np,rate,momentum,epsilon. Article: February pp.35-36."; };
LABEL { ADDR $C003; NAME "BP_Recognize"; SIZE 1; COMMENT "SYS 49155,pattern. Results are left in O2() and O3(). February p.36."; };
LABEL { ADDR $C006; NAME "BP_TrainPattern"; SIZE 1; COMMENT "Extra vector, not described in the article: parse a stored pattern number and train it."; };
LABEL { ADDR $C009; NAME "BP_TrainEpoch"; SIZE 1; COMMENT "Extra vector, not described in the article: one complete training pass and TE update."; };
LABEL { ADDR $C00C; NAME "BP_Learn"; SIZE 1; COMMENT "SYS 49164,show_error. Repeat training passes until TE <= EP or RUN/STOP."; };
LABEL { ADDR $C00F; NAME "BP_DefinePair"; SIZE 1; COMMENT "SYS 49167,number,input_string,teacher_string. February p.36."; };
LABEL { ADDR $C012; NAME "BP_Save"; SIZE 1; COMMENT "SYS 49170,filename. Save dimensions, parameters, weights, momentum and patterns."; };
LABEL { ADDR $C015; NAME "BP_Load"; SIZE 1; COMMENT "SYS 49173,filename. Recreate BASIC storage, then restore the saved network."; };
LABEL { ADDR $C018; NAME "InitializeNetwork"; SIZE 1; COMMENT "Parse four byte-sized dimensions. Store rate, momentum and epsilon through BASIC scalar lookup."; };
LABEL { ADDR $C0E2; NAME "CreateBasicStorage"; SIZE 1; COMMENT "Create P1/P2/P3/NP/TE and DIM eleven arrays using the embedded expression list."; };
LABEL { ADDR $C17E; NAME "SetHiddenBias"; SIZE 1; COMMENT "O2(0)=1 and O3(0)=1. BASIC arrays include index zero; active PEs start at index one."; };
LABEL { ADDR $C1C7; NAME "SetInputBiases"; SIZE 1; COMMENT "Set IN(0,pattern)=1, including scratch pattern zero, for bias-weight multiplication."; };
LABEL { ADDR $C203; NAME "InitializeW1"; SIZE 1; COMMENT "Initialize all allocated W1 entries, including index-zero slots."; };
LABEL { ADDR $C222; NAME "RandomizeW1Element"; SIZE 1; COMMENT "Observed formula: 10*RND(1)-5, using BASIC five-byte floating point."; };
LABEL { ADDR $C270; NAME "InitializeW2"; SIZE 1; COMMENT "Initialize the second weight matrix with the same random distribution."; };
LABEL { ADDR $C2DD; NAME "FindRemainingArrays"; SIZE 1; COMMENT "Cache the error, teacher and momentum array addresses after DIM."; };
LABEL { ADDR $C343; NAME "MakeOffsetsRelative"; SIZE 1; COMMENT "Convert cached array pointers to ARYTAB-relative offsets and scalar pointers to VARTAB-relative offsets."; };
LABEL { ADDR $C44D; NAME "RecognizeString"; SIZE 1; COMMENT "Import the supplied string into IN(:,0), then perform the forward pass on scratch pattern zero."; };
LABEL { ADDR $C457; NAME "SelectStoredPattern"; SIZE 1; COMMENT "Parse and range-check an existing pattern number in 1..NP."; };
LABEL { ADDR $C46F; NAME "ForwardPass"; SIZE 1; COMMENT "Compute hidden outputs, output outputs, and E(pattern). Training also enters here directly."; };
LABEL { ADDR $C474; NAME "HiddenNeuron"; SIZE 1; COMMENT "Accumulate W1(hidden,input)*IN(input,pattern), including input zero for bias."; };
LABEL { ADDR $C4E1; NAME "HiddenSigmoid"; SIZE 1; COMMENT "Compute 1/(1+EXP(-sum)) with the embedded floating-point zero and one constants."; };
LABEL { ADDR $C52B; NAME "OutputLayer"; SIZE 1; COMMENT "Repeat the weighted sum and sigmoid using W2 and O2, including O2(0)=1."; };
LABEL { ADDR $C5E1; NAME "PatternSquaredError"; SIZE 1; COMMENT "E(pattern)=sum((T(output,pattern)-O3(output))^2)/2. Recognition uses scratch teacher column zero."; };
LABEL { ADDR $C690; NAME "IndexVector"; SIZE 1; COMMENT "Add 5*A to ElementPtr. BASIC floating-point array elements occupy five bytes."; };
LABEL { ADDR $C6AA; NAME "IndexMatrix"; SIZE 1; COMMENT "Add 5*(X*(A+1)+Y) to ElementPtr. A is the first dimension upper bound; zero is allocated."; };
LABEL { ADDR $C6E3; NAME "PrintFloat"; SIZE 1; COMMENT "Format FAC in BASIC_PRINT_BUFFER and send it through BASIC string output."; };
LABEL { ADDR $C6FE; NAME "TrainSelectedPattern"; SIZE 1; COMMENT "Parse stored pattern, run the forward pass, then adjust weights."; };
LABEL { ADDR $C704; NAME "TrainCurrentPattern"; SIZE 1; COMMENT "Internal entry: PatternIndex is already set by the epoch loop."; };
LABEL { ADDR $C707; NAME "OutputDeltaLoop"; SIZE 1; COMMENT "E3(k)=(T(k,p)-O3(k))*O3(k)*(1-O3(k)). Process each output neuron."; };
LABEL { ADDR $C7A1; NAME "UpdateW2"; SIZE 1; COMMENT "M2(k,j)=rate*E3(k)*O2(j)+momentum*M2(k,j); then W2(k,j)+=M2(k,j), including bias j=0."; };
LABEL { ADDR $C88B; NAME "HiddenDeltaLoop"; SIZE 1; COMMENT "E2(j)=O2(j)*(1-O2(j))*sum(E3(k)*W2(k,j)). Uses W2 as currently stored after its updates."; };
LABEL { ADDR $C95A; NAME "UpdateW1"; SIZE 1; COMMENT "M1(j,i)=rate*E2(j)*IN(i,p)+momentum*M1(j,i); then W1(j,i)+=M1(j,i), including bias i=0."; };
LABEL { ADDR $CA4B; NAME "TrainEpoch"; SIZE 1; COMMENT "Clear TE, train patterns 1..NP in order, and accumulate their pre-update E(pattern) values."; };
LABEL { ADDR $CAB0; NAME "LearnUntilTolerance"; SIZE 1; COMMENT "Parse show-error flag and cache the EP address. The article uses this entry for complete learning."; };
LABEL { ADDR $CACA; NAME "LearningPass"; SIZE 1; COMMENT "One epoch, RUN/STOP poll, optional TE print, and comparison with epsilon."; };
LABEL { ADDR $CB04; NAME "DefineTrainingPair"; SIZE 1; COMMENT "Validate pattern number, import input and teacher strings into the corresponding BASIC arrays."; };
LABEL { ADDR $CB1C; NAME "ReadInputString"; SIZE 1; COMMENT "Require length P1. Each ASCII 1 becomes numeric one; every other character becomes zero."; };
LABEL { ADDR $CB8A; NAME "ReadTeacherString"; SIZE 1; COMMENT "Require length P3. The implementation uses the same ASCII-1 test as the input parser."; };
LABEL { ADDR $CBF7; NAME "SaveNetwork"; SIZE 1; COMMENT "Sequential disk file on device 8; logical file 1, secondary address 2. Append comma-W to the name."; };
LABEL { ADDR $CC49; NAME "WriteNetworkBody"; SIZE 1; COMMENT "Write P1/P2/P3/NP, RA/MO/EP, then W1/W2/M1/M2/IN/T. This is not a PRG save."; };
LABEL { ADDR $CCF9; NAME "WriteScalar"; SIZE 1; COMMENT "Resolve VARTAB-relative scalar offset and write its five stored bytes."; };
LABEL { ADDR $CD11; NAME "WriteMatrix"; SIZE 1; COMMENT "Resolve ARYTAB-relative matrix offset; write (bound1+1)*(bound2+1) five-byte elements."; };
LABEL { ADDR $CD4D; NAME "DiskError"; SIZE 1; COMMENT "Print drive status through carriage return, close channels, reset BASIC stack, return to READY."; };
LABEL { ADDR $CD63; NAME "LoadNetwork"; SIZE 1; COMMENT "Append comma-R, read dimensions, rebuild BASIC storage, then read parameters and matrices."; };
LABEL { ADDR $CE65; NAME "ReadScalar"; SIZE 1; COMMENT "Read five bytes into a VARTAB-relative scalar."; };
LABEL { ADDR $CE7D; NAME "ReadMatrix"; SIZE 1; COMMENT "Read all matrix elements including allocated row/column zero."; };
LABEL { ADDR $CEB9; NAME "RecreateForLoad"; SIZE 1; COMMENT "Reset BASIC array/string boundaries and recreate the network using saved dimensions."; };
LABEL { ADDR $CF0E; NAME "OpenStatusChannel"; SIZE 1; COMMENT "Open logical file 15 on device 8, secondary address 15."; };
LABEL { ADDR $CF20; NAME "CloseNetworkFiles"; SIZE 1; COMMENT "Restore default channels and close logical files 1 and 15."; };
LABEL { ADDR $CF2E; NAME "DimExpressions"; SIZE 1; COMMENT "BASIC DIM argument text, not executable 6502 instructions."; };
LABEL { ADDR $CF8B; NAME "NameRate"; SIZE 1; COMMENT "BASIC stores only the first two characters of a variable name: RA is RATE."; };
LABEL { ADDR $CF8E; NAME "NameMomentum"; SIZE 1; COMMENT "MO is MOMENTUM."; };
LABEL { ADDR $CF91; NAME "NameEpsilon"; SIZE 1; COMMENT "EP is EPSILON."; };
LABEL { ADDR $CF94; NAME "NameP1"; SIZE 1; COMMENT "Input dimension."; };
LABEL { ADDR $CF97; NAME "NameP2"; SIZE 1; COMMENT "Hidden dimension."; };
LABEL { ADDR $CF9A; NAME "NameP3"; SIZE 1; COMMENT "Output dimension."; };
LABEL { ADDR $CF9D; NAME "NameNP"; SIZE 1; COMMENT "Pattern count."; };
LABEL { ADDR $CFA0; NAME "NameO1Unused"; SIZE 1; COMMENT "Embedded O1(0) text retained from the binary; not part of the DIM list."; };
LABEL { ADDR $CFA6; NAME "NameO2"; SIZE 1; COMMENT "Lookup text for hidden outputs, starting at index zero."; };
LABEL { ADDR $CFAC; NAME "NameO3"; SIZE 1; COMMENT "Lookup text for output outputs."; };
LABEL { ADDR $CFB2; NAME "NameInput"; SIZE 1; COMMENT "Lookup text for IN(0,0)."; };
LABEL { ADDR $CFBA; NAME "NameW1"; SIZE 1; COMMENT "Lookup text for W1(0,0)."; };
LABEL { ADDR $CFC2; NAME "NameW2"; SIZE 1; COMMENT "Lookup text for W2(0,0)."; };
LABEL { ADDR $CFCA; NAME "NameE2"; SIZE 1; COMMENT "Lookup text for hidden deltas."; };
LABEL { ADDR $CFD0; NAME "NameE3"; SIZE 1; COMMENT "Lookup text for output deltas."; };
LABEL { ADDR $CFD6; NAME "NameTeacher"; SIZE 1; COMMENT "Lookup text for teacher patterns."; };
LABEL { ADDR $CFDD; NAME "NameM1"; SIZE 1; COMMENT "Lookup text for prior W1 changes."; };
LABEL { ADDR $CFE5; NAME "NameM2"; SIZE 1; COMMENT "Lookup text for prior W2 changes."; };
LABEL { ADDR $CFED; NAME "NameError"; SIZE 1; COMMENT "Lookup text for per-pattern error."; };
LABEL { ADDR $CFF2; NAME "FloatZero"; SIZE 5; COMMENT "Commodore five-byte floating-point zero."; };
LABEL { ADDR $CFF7; NAME "FloatOne"; SIZE 5; COMMENT "Commodore five-byte floating-point one: exponent byte $81, zero mantissa/sign bytes."; };
LABEL { ADDR $CFFC; NAME "NameTotalError"; SIZE 1; COMMENT "Null-terminated TE name. The original file stops at $CFFE; printed $CFFF is absent."; };