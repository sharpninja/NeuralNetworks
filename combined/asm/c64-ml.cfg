# Preserve the PRG header and the fixed $C000 payload without padding.
MEMORY {
    HEADER: start = $BFFE, size = $0002, file = %O;
    RAM:    start = $C000, size = $1000, file = %O;
}
SEGMENTS {
    LOADADDR: load = HEADER, type = ro;
    CODE:     load = RAM,    type = ro;
}
